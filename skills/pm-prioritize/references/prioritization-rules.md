# Prioritization Rules

## 目标

优先级排序的目标不是公平对待所有功能，而是保护这个版本的验证目标。

## 基本原则

- 先定义“这个版本要验证什么”
- 只保留与验证目标强相关的功能
- 没有强证据支持的功能默认延后
- 本版不做清单必须明确写出来

## 推荐框架

- `P0`：不做就无法验证核心价值
- `P1`：做了更好，但不影响首版成立
- `P2`：可以延后到下一轮

## 常见误区

- 把“老板想要”直接当成 P0
- 把未来可能有用的功能全带上
- 用“都重要”回避取舍
